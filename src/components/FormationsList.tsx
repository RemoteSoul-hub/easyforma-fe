Here's the fixed version with all missing closing brackets added:

```jsx
// ... [previous code remains the same until the last few sections]

                    <div className="text-center mt-12 pt-8">
                      <p className="text-xl text-purple-100 font-semibold">
                        En bref : tu peux revendre ce que tu viens d'acheter.
                      </p>
                    </div>
                  </div>
                </div>
              </div>
            </div>
          </div>

          {/* Dimanche Business Section */}
          <div className="my-20">
            {/* ... [Dimanche Business section content] ... */}
          </div>

        </div>
      </div>
    </section>
  );
};

export default FormationsList;
```

I've added the missing closing brackets for multiple nested divs and the component itself. The structure is now properly closed with all elements having their corresponding closing tags.